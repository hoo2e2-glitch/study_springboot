<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.restful.mapper.MemberMapper">
    <!--    추가-->
    <insert id="insert" parameterType="MemberVO">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE" >
            SELECT SEQ_MEMBER.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_MEMBER
        VALUES (#{id}, #{memberEmail},#{memberPassword}, #{memberName})
    </insert>

    <!-- 이메일 유무 조회-->
    <select id="existMemberEmail" resultType="int" parameterType="String">
        SELECT COUNT(*)
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
    </select>
    <!-- 전체 조회 -->
    <select id="selectAll" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME
        FROM TBL_MEMBER
    </select>
    <!-- -->
    <select id="selectById" parameterType="Long" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME
        FROM TBL_MEMBER
        WHERE ID = #{id}
    </select>

<!-- email/password로 단일조회-->
    <select id="selectByMemberEmailAndMemberPassword" parameterType="MemberVO" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
          AND MEMBER_PASSWORD = #{memberPassword}
    </select>

<!--    // 회원정보 수정-->
    <update id="update" parameterType="MemberVO" >
        UPDATE TBL_MEMBER
        SET MEMBER_NAME = #{memberName},
            MEMBER_PASSWORD = #{memberPassword}
        WHERE ID = #{id}
    </update>

    <!--비밀번호 수정-->
    <update id="updatePassword" parameterType="MemberVO" >
        UPDATE TBL_MEMBER
            MEMBER_PASSWORD = #{memberPassword}
        WHERE ID = #{id}
    </update>
    <!--삭제-->
    <delete id="delete" parameterType="Long">
        DELETE FROM TBL_MEMBER
        WHERE ID = #{id}
    </delete>

</mapper>

        <!--    추가-->
        <!--    <insert id="insert" parameterType="MemberJoinRequestDTO">-->
        <!--        INSERT INTO TBL_MEMBER-->
        <!--        VALUES (SEQ_MEMBER.NEXTVAL, #{memberEmail},#{memberPassword}, #{memberName})-->
        <!--    </insert>-->
        <!--    id로 단일조회-->